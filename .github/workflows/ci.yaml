name: CI

on:
  push:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

jobs:
  ci:
    uses: augentic/.github/.github/workflows/ci.yaml@main
    secrets: inherit
    with:
      targets: wasm32-wasip2

  wasm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-wasip2
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          # rust-cache only saves on a key miss, so re-key whenever the build
          # steps change or the cache never picks up newly built dependencies.
          key: ${{ hashFiles('Makefile.toml', '.github/workflows/ci.yaml') }}
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-make
      - run: cargo make lint-wasm
